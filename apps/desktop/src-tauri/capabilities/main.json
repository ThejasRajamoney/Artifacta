{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal permissions for the bundled Artifacta main window",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default"
  ]
}
